{
  "name": "vue-symbols",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vp run -r build",
    "check": "vp check",
    "clear": "git clean -fdX node_modules && vp run -r clean",
    "fix": "vp check --fix"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "vite-plus": "catalog:",
    "zx": "8.8.5"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.33.0"
}
