{
  "name": "vue-symbols",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vp run -r build",
    "check": "vp check",
    "clean": "git clean -fdX node_modules && vp run -r clean",
    "fix": "vp check --fix",
    "release": "vp run @vue-symbols/icons#release"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "vite-plus": "catalog:",
    "zx": "8.8.5"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.33.0"
}
